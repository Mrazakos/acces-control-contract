# Benchmark Data

This directory stores historical benchmark results for regression tracking.

## Structure

- `benchmark-latest.json` - Most recent benchmark run
- `benchmark-<timestamp>.json` - Timestamped benchmark snapshots
- `historical/` - Archived benchmarks tagged by git commit

## Usage

Compare current performance against historical data to detect regressions.
